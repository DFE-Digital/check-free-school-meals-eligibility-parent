@model CheckYourEligibility.Admin.ViewModels.DeleteReportConfirmationViewModel
@{
    ViewData["Title"] = $"Delete report generated by {Model.GeneratedBy} on {Model.GeneratedDate:dd MMMM yyyy}";
}

<div class="govuk-grid-column-full">
    <a class="govuk-back-link backLinkJS" href="@Url.Action("Reports", "EligibilityCheckReporting")">Back</a>
    
    <h1 class="govuk-heading-l">@ViewData["Title"]</h1>
    <div class="govuk-warning-text">
        <span class="govuk-warning-text__icon" aria-hidden="true">!</span>
        <strong class="govuk-warning-text__text">
            <span class="govuk-visually-hidden">Warning</span>
            This action cannot be undone. Your organisation will no longer be able to view this report if you delete it.
        </strong>
    </div>
    <div class="govuk-!-padding-bottom-3"></div>
    <div class="govuk-button-group">
        <form method="post" action="@Url.Action("Delete_Report", "EligibilityCheckReporting")">
            <input type="hidden" name="reportId" value="@Model.ReportID" />

            <button type="submit" class="govuk-button govuk-button--warning">
                Delete report
            </button>
        </form>
        <a href="@Url.Action("Reports", "EligibilityCheckReporting")" role="button" draggable="false" class="govuk-button govuk-button--secondary" data-module="govuk-button" data-govuk-button-init="">
            Cancel and return to reports
        </a>
    </div>
    <div class="govuk-!-padding-bottom-3"></div>
</div>
